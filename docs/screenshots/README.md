# Screenshot placeholders

The catalog orchestrator will add the real captures at the final pinned commit:

- `web-board.png` — Boardstate Web tab with an agent-built board.
- `desktop-board.png` — Boardstate Desktop page using the plugin SDK transport.
- `native-tools.png` — Hermes tool inventory showing all 19 `boardstate_*` tools.
